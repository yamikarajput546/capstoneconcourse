---
resources:
  - name: code
    type: git
    source: 
      uri: https://github.com/yamikarajput546/capstoneconcourse
      branch: main

jobs:
  - name: compile
    plan: 
      - get: code
        trigger: true
      - task: clean-compile
        config: 
          platform: linux
          image_resource:
            type: docker-image
            source: 
              repository: maven
          inputs:
            - name: code
          run: 
            path: mvn
            args: 
              - package
              - -f
              - code/pom.xml 